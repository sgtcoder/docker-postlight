<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

    <link type="image/x-icon" href="https://www.google.com/s2/favicons?domain={{domain}}" rel="shortcut icon">

    <style>
        :root {
            --fg: #000;
            /* foreground  */
            --bg: #fff;
            /* background  */
            --bd: #000;
            /* border  */
            --lk: #006fa5;
            /* link */
            --lkv: #d33bf0;
            /* link visited */
            --hg: #ffff81;
            /* highlight  */
        }

        html {
            overflow: overlay;
        }

        body {
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            margin: 0 auto;
            padding: 25px;
            position: relative;
            color: var(--fg);
            border-color: var(--bd);
            background-color: var(--bg);
            font-size: 24px;
            font-family: 'Open Sans', sans-serif;
            max-width: 1000px;
        }

        body[data-mode$=dark] {
            --lk: #0096de;
        }

        body[data-mode=light] {
            color-scheme: light;
            --fg: #222;
            --bd: #222;
            --bg: whitesmoke;
        }

        body[data-mode=dark] {
            color-scheme: dark;
            --fg: #eee;
            --bd: #eee;
            --bg: #333;
        }

        body[data-mode=sepia] {
            color-scheme: light;
            --fg: #5b4636;
            --bd: #5b4636;
            --bg: #f4ecd8;
            --lkv: #5b4636;
        }

        body[data-mode=solarized-sepia] {
            color-scheme: light;
            --fg: #222222;
            --bd: #222222;
            --bg: #e3dfd7;
            --lk: #006fa5;
            --lkv: #006fa5;
        }

        body[data-mode=solarized-light] {
            color-scheme: light;
            --fg: #586e75;
            --bd: #586e75;
            --bg: #fdf6e3;
        }

        body[data-mode=nord-light] {
            color-scheme: light;
            --fg: #2e3440;
            --bd: #2e3440;
            --bg: #e5e9f0;
        }

        body[data-mode=groove-dark] {
            color-scheme: dark;
            --fg: #cec4ac;
            --bd: #cec4ac;
            --bg: #282828;
        }

        body[data-mode=solarized-dark] {
            color-scheme: dark;
            --fg: #839496;
            --bd: #839496;
            --bg: #002b36;
        }

        body[data-mode=nord-dark] {
            color-scheme: dark;
            --fg: #e5e9f0;
            --bd: #e5e9f0;
            --bg: #2e3440;
        }

        body[data-loaded=true] {
            transition: color .4s, background-color .4s;
        }

        img {
            max-width: 100%;
            height: auto;
        }

        img:not([width])[src$=".svg"] {
            max-width: 100px;
        }

        #published-time:empty,
        #reader-credits:empty,
        body[data-images=false] canvas,
        body[data-images=false] img,
        body[data-images=false] svg {
            display: none;
        }

        svg:not([width]):not([height]) {
            max-width: 25vmin;
        }

        a {
            color: var(--lk);
            text-decoration: none;
        }

        a:visited {
            color: var(--lkv);
        }

        hr {
            background-color: var(--bd);
            height: 1px;
            border: 0;
        }

        #reader-domain {
            font-family: Helvetica, Arial, sans-serif;
            text-decoration: none;
            border-bottom-color: currentcolor;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            width: 100%;
            display: inline-block;
            direction: ltr;
        }

        #reader-domain>span:first-child {
            font-size: 1.1em;
        }

        #reader-domain>span:last-child {
            font-size: .8em;
        }

        #reader-title {
            font-size: 1.6em;
            line-height: 1.25em;
            width: 100%;
            margin: 10px 0 20px;
            padding: 0;
        }

        #doi,
        #published-time,
        #reader-credits,
        #reader-estimated-time {
            font-size: .85em;
            line-height: 1.48em;
            margin: 0;
            padding: 0;
        }

        #doi-json {
            overflow: hidden;
        }

        #doi-json code {
            overflow: auto;
            max-height: 400px;
        }

        mark.hghlght {
            background-color: var(--hg);
        }

        body:not([data-columns="1"]) .page :is(h1, h2) {
            column-span: all;
            background-color: rgba(125, 125, 125, .1);
            padding: 10px;
        }

        .hidden {
            display: none;
        }

        pre {
            white-space: pre-wrap;
        }

        pre code {
            background-color: #eff0f1;
            color: #393318;
            font-family: monospace;
            display: block;
            padding: 5px 10px;
        }

        body[data-mode=dark] pre code {
            background-color: #585858;
            color: #e8e8e8;
        }

        p {
            text-align: left;
        }

        .page {
            line-height: 36px;
            column-count: unset;
        }

        h1,
        h2,
        h3 {
            line-height: initial;
        }

        iframe {
            aspect-ratio: 16/9;
            width: 100%;
            height: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            overflow-x: auto;
            display: block;
        }

        td,
        th {
            border: 1px solid #000;
            padding: 0 5px;
        }

        #toc_container {
            display: none;
        }

        blockquote {
            border-left-style: solid;
            border-left-width: 7px;
            padding-left: 20px;
            font-size: 1.235em;
            margin-inline-start: 0;
        }

        figure {
            margin: 20px 0;
        }

        .reader-favicon {
            display: inline-block;
            vertical-align: middle;
            width: 16px;
        }

        .reader-domain {
            display: inline-block;
            color: #696969;
            font-size: 14px;
        }

        @media print {
            body[data-mode] {
                --fg: #000;
                --bd: #000;
                --bg: #fff;
                width: unset;
                padding: 0;
                margin: 0;
                font-size: 16px;
                line-height: 28px;
            }

            iframe {
                display: none;
            }

            .page {
                line-height: 28px;
            }
        }

        @media (max-width: 768px) {
            body {
                font-size: 20px;
                line-height: 28px;
                padding: 15px;
            }

            .page {
                line-height: 28px;
            }
        }
    </style>
</head>

<body data-images="true" data-mode="solarized-sepia" data-columns="1" data-loaded="true" tabindex="1">
<div>
    <img class="reader-favicon" src="https://www.google.com/s2/favicons?domain={{domain}}" alt="{{domain}}">

    <h3 class="reader-domain">{{domain}}</h3>
</div>

<h1 id="reader-title">
    <a href="{{url}}" target="_blank">{{title}}</a>
</h1>

<div id="reader-credits">{{author}}</div>

<span id="published-time">{{formattedPublishedTime}}</span>

{{#ifCond formattedPublishedTime '!=' ''}}
    {{#ifCond word_count '>' 100}}
        <span> | </span>
    {{/ifCond}}
{{/ifCond}}

<span id="reader-estimated-time">
    {{#ifCond word_count '>' 100}}
        {{readingTime}} min read
    {{/ifCond}}
</span>


<hr />

<div id="reader-content">
    {{encodeMyString content}}
</div>
</body>

</html>